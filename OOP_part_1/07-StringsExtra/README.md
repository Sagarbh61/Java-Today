**Java Strings – Extra Practice Programs**

These programs help to understand:
- String immutability
- String manipulation techniques
- Clean logic and input handling

------------------------------------------------------------------------------------------------------------------------------------------

📌 Programs Included

1. Palindrome String
Checks whether a given string is a palindrome.
- Case-insensitive
- Ignores extra spaces
- Uses `StringBuilder` for efficient reversal

2. Reverse String
Reverses the entire string character by character.

3. Reverse Words in a Sentence
Reverses the order of words, not characters.

4. Count Vowels
Counts the number of vowels (`a, e, i, o, u`) in a given string.
- Case-insensitive

5. Count Words
Counts total words in a sentence.
- Uses space normalization (`\\s+`)

6. Count Characters (Ignoring Spaces)
Counts total characters excluding whitespace.

7. Anagram Check
Checks whether two strings are anagrams.
- Case-insensitive
- Ignores spaces
- Implemented using simple String operations (no arrays / HashMap)

------------------------------------------------------------------------------------------------------------------------------------------

📝 Notes

- Input normalization (`toLowerCase()`, removing spaces) is done before logic.
- Programs are written for clarity first, optimization second.

🚀 Purpose

These programs strengthen Java fundamentals and prepare the base for:
- OOP concepts
- Mini projects
- Interview-ready problem solving
------------------------------------------------------------------------------------------------------------------------------------------